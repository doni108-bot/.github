#!/bin/sh
# script/cibuild: run continuous integration checks

set -e

cd "$(dirname "$0")/.."

echo "==> Installing dependencies..."
npm install

echo "==> Running tests..."
npm test

echo "==> All checks passed."
